<%= form_with(model: room, local: true) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :white_id %>
    <%= form.number_field :white_id, id: :room_white_id %>
  </div>

  <div class="field">
    <%= form.label :black_id %>
    <%= form.number_field :black_id, id: :room_black_id %>
  </div>

  <div class="field">
    <%= form.label :side %>
    <%= form.number_field :side, id: :room_side %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
